# EHS MCP Server — Python dependencies
# Install with: pip install -r requirements.txt

mcp

# HTTP client (async)
httpx>=0.27.0

# SSE transport / web server (needed only for --transport sse)
starlette>=0.40.0
uvicorn>=0.30.0

# Document processors
pdfplumber>=0.11.0          # PDF extraction (text + tables)
python-docx>=1.1.0          # .docx extraction
openpyxl>=3.1.0             # .xlsx extraction
xlrd>=2.0.1                 # legacy .xls extraction

# Image OCR
pytesseract>=0.3.10         # Tesseract Python wrapper
Pillow>=10.0.0              # Image loading

# Text encoding detection
chardet>=5.2.0

# Utilities
python-dotenv>=1.0.0        # Load .env file
